export const environment = {
  production: true,
  apiUrl: 'https://pgo-api.otapp.live/api', // Production API URL
  enableDevAuth: false, // Disable development authentication bypass in production
  devCredentials: null
};
